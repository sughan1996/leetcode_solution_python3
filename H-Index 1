class Solution:

    def hIndex(self, citations: List[int]) -> int:
        
        count = 0
        
        citations.sort()
        
        
        for i in range(len(citations)):
        
            if citations[i] > = len(citations[i:]):
            
                count = max(count, len(citations[i:]))
                
                
        return count
        
